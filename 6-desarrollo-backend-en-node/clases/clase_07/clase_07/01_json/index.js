const fs = require('fs');

/* const miAuto = {
  marca: 'BMW',
  modelo: 'A50'
};

fs.writeFileSync('json/auto.json', JSON.stringify(miAuto), 'utf8'); */

/* const yo = {
  nombre: 'Raul',
  apellido: 'Farias'
};

fs.writeFileSync('json/yo.json', JSON.stringify(yo)); */

/* fs.writeFileSync('json/persona.json', JSON.stringify({ name: 'Raul 2', lastname: 'Farias 2'})); */

/* const file = JSON.parse(fs.readFileSync('json/auto.json', 'utf8'));
console.log(file.modelo); */

/* const file = JSON.parse(fs.readFileSync('json/yo.json', 'utf8'));
console.log(file.nombre, file.apellido); */
